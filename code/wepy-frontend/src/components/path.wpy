<style lang="less">
  .path{
    left: 34rpx;
    top: 180rpx;
    width: 682rpx;
    height: 200rpx;
    line-height: 40rpx;
    border-radius:16rpx;
    background-color: rgb(242, 242, 242);
    text-align: center;
    display: flex;
    flex-direction: row;
  }

  .pathimg{
    position:relative;
    width:126rpx;
    height:126rpx;
    overflow:hidden;
    border-radius:50%;
  }

  .pathimg image{
    width:100%;
    height:auto;
  }
  .pathname{
    font-size: 18px;
    font-weight: bold;
    color: #111111;
  }
  .pathdetail{
    font-size: 16px;
    color: #222222;
  }
</style>

<template>
  <view @tap = "notifi" class="path">
    <view class="pathimg">
      <image mode="widthFix" src="{{path.img}}" />
    </view>
    <view>
      <text class="pathname">{{path.name}}</text>
    </view>
    <view>
      <text class="pathdetial">步数：{{path.steps}}  转弯数：{{path.curves}}</text>
    </view>
  </view>
</template>

<script>
  import wepy from 'wepy';

  export default class Path extends wepy.component {
    props = {
      path:{
        type:Object,
        default:{
          img:"",
          name:"",
          steps:0,
          curves:0
        }
      }
    }

    method = {
      notifi () {
        console.log(path.img)
        this.$emit('tap-path',this.index)
      }
    }
  }
</script>
